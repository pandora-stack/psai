# Rust build artifacts (53 MB+) — never commit
vault/target/
**/target/
# release binaries — attach to GitHub releases, don't commit
vault/dist/

# Secrets / local-only — never commit
pass
.stack.env
secrets/
.runtime.env
*.7z
*.7z.sha256
# local host/creds scratch file (SSH target for build/test) — never commit
docs/data
docs/data.*

# local dev-only agent/work notes — keep out of published main
AGENTS.md
dev/

# OS noise
.DS_Store
**/.DS_Store
